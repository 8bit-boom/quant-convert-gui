# GUI itself
gradio>=5.0,<7

# Lets the GUI download a model straight from a Hugging Face URL
huggingface_hub>=0.24

# --- The actual quantization engine (does the real work) ---
# PyTorch is NOT included here on purpose: install the build that matches
# your GPU/CUDA from https://pytorch.org/get-started/locally/ first, e.g.
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
# Installed from the 8bit-boom main branch rather than PyPI: that build has
# the native stop/resume checkpoint support (--checkpoint-dir / --stop-file)
# the GUI's Pause/Stop/Resume buttons use. PyPI releases of convert-to-quant
# work too, but stopping a run then falls back to restart-from-scratch.
convert-to-quant @ git+https://github.com/8bit-boom/convert_to_quant@main

# ctq's own installed package metadata doesn't reliably pull these in on
# every platform (seen missing on both Linux and Windows installs) - install
# them explicitly or ctq crashes with "ModuleNotFoundError" the first time
# it actually needs them (safetensors: always; scipy: only with --convrot).
safetensors>=0.4.2
scipy

# Optional, speeds up INT8 kernels on Linux (use triton-windows on Windows,
# see the Environment tab in the app for the exact command).
# triton
